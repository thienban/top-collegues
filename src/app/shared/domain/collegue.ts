export class Collegue {
  private _nom: string;
  private _URL: string;
  private _score: number;

  constructor(nom, url, score) {
    this._nom = nom;
    this._URL = url;
    this._score = score;
  }
}
